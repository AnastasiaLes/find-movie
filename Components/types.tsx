export type MovieType = {
  id: string;
  title: string;
  releaseYear: string;
  poster_path: string;
  release_date: string;
  overview: string;
};
export type SearchText = {
  searchText: string;
};
